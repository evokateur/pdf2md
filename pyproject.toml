[project]
name = "pdf2md"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pymupdf4llm"]

[project.scripts]
pdf2md = "pdf2md:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
