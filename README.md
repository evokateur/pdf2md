# pdf2md

Command line tool that generates markdown from PDF files using [`PyMuPDF4LLM`](https://github.com/pymupdf/PyMuPDF4LLM), with link extraction and minimal cleanup and normalization.

```sh
make install # runs uv tool install . --reinstall
```

```sh
pdf2md example.pdf > output.md
```
